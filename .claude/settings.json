{
  "permissions": {
    "allow": [
      "mcp__puppeteer__puppeteer_navigate",
      "mcp__puppeteer__puppeteer_screenshot",
      "mcp__puppeteer__puppeteer_click",
      "mcp__puppeteer__puppeteer_fill",
      "mcp__puppeteer__puppeteer_evaluate",
      "mcp__puppeteer__puppeteer_select",
      "mcp__puppeteer__puppeteer_hover",
      "Bash(curl:*)",
      "Bash(npm run serve:*)",
      "Bash(npm run build:*)",
      "Bash(rm:*)",
      "Bash(curl -X POST:*)",
      "Bash(pkill:*)"
    ]
  },
  "enableAllProjectMcpServers": true,
  "enabledMcpjsonServers": [
    "puppeteer"
  ],
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "WebSearch",
        "hooks": [
          {
            "type": "command",
            "command": "export SESSION_ID=$(uuidgen 2>/dev/null || echo $RANDOM-$(date +%s)) && actions/thinking.sh && actions/idle.sh"
          }
        ]
      }
    ],
    "PostToolUse": [
        {
        "hooks": [
          {
            "type": "command",
            "command": "export SESSION_ID=$(uuidgen 2>/dev/null || echo $RANDOM-$(date +%s)) && actions/happy.sh"
          }
        ]
      }
    ],
    "Notification": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "export SESSION_ID=$(uuidgen 2>/dev/null || echo $RANDOM-$(date +%s)) && actions/requesting_permissions.sh && actions/idle.sh"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "export SESSION_ID=$(uuidgen 2>/dev/null || echo $RANDOM-$(date +%s)) && actions/operation_complete.sh && actions/idle.sh"
          }
        ]
      }
    ]
  }
}